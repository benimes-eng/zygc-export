# Copy to .env for local development. On Netlify, set these in:
# Site settings → Environment variables → Add a variable

# Public site URL (used for canonical links and OG metadata if configured)
VITE_SITE_URL=https://your-site.netlify.app

# Optional: override the default WhatsApp contact number
# VITE_WHATSAPP_NUMBER=251911247873
